export interface IRefreshTokenResponse {
  accessToken: string;
}

export interface IGetUserResponse {
  id: string;
  fullName: string;
  email: string;
  role: string;
  isActive: boolean;
  lastLoginAt: string;
  lastLoginIP: string;
  createdAt: string;
  updatedAt: string;
}
